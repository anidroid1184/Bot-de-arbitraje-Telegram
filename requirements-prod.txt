# Production dependencies (Linux/WSL/Docker)
# Core
aiohttp==3.10.5  # optional if we later use async HTTP; harmless to have ready
selenium==4.15.2
requests==2.31.0
python-telegram-bot==20.7

# Parsing / data
beautifulsoup4==4.12.2
lxml==5.2.2
pandas>=2.2.0

# Config
python-dotenv==1.0.0
pyyaml==6.0.1

# Logging
structlog==23.2.0

# Network capture (for Betburger/Surebet tokens)
selenium-wire==5.1.0
setuptools>=68.0.0
packaging>=23.2

# OCR (opcional, requiere tesseract en el sistema)
pytesseract==0.3.10
Pillow==10.3.0
